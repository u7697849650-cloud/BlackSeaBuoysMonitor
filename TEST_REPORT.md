# Тестов отчет

Добавени са unit тестове за валидна таблица, разместени и липсващи колони, празни/невалидни стойности, десетична запетая и класификация на актуалността.

Проверка на 18.07.2026:

* `testDebugUnitTest` — успешно;
* `assembleDebug` — успешно;
* `assembleRelease` и lintVital — успешно;
* инсталация на физически телефон — не е изпълнена, защото няма свързано Android устройство.

## Open-Meteo Marine

Реална заявка за Варна на 18.07.2026 върна 168 почасови точки. Потвърдена примерна точка: `2026-07-18T00:00`, вълна `0.18 m`, температура на морската повърхност `25.4 °C`, timezone `Europe/Sofia`.

Добавените тестове покриват DTO JSON parsing, различни дължини на масивите, липсващи стойности, невалидни дати, 16-посочен компас, най-близък бъдещ час и дневно обобщение. `testDebugUnitTest`, `assembleDebug`, `assembleRelease` и release lint преминават успешно.

Добавени са проверки на evaluation engine за всички 12 активности, опасни прагове, липсващи/стари/кеширани данни, персонален праг за лодка и сливане на последователни подходящи часове.

Advertising тестовете проверяват globally disabled поведението, user opt-out, фонова реклама по подразбиране, opacity clamp/effective minimum, HTTPS URL защита, периоди, placement/destination/activity filters, priority, dismissal, session interval/cap и пропускане на невалидна кампания.
